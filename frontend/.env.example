# The URL of your backend API.
# If deploying via docker-compose on the same server, you might want to point this
# to the server's public IP or domain name, e.g., http://your-server-ip:8000
VITE_API_URL=http://localhost:8000
